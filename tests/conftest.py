"""Test configuration for StrucIn.

The package must be installed in editable mode before running the test suite::

    pip install -e ".[dev]"

No ``sys.path`` manipulation is needed when the editable install is in place.
"""

from __future__ import annotations
